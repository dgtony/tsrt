extern crate tsrt;

//use tsrt::{dfs, Relation, TSortErr};
//use tsrt::{Relation, TSortErr};
use tsrt::TSortErr;

fn main() {
    //    let rel = Relation { from: "a", to: "b" };
    //
    //    let rels = vec![rel];
    //
    //    //let fs = rels.iter().next();
    //
    //    let res = dfs::sort(rels.iter());
    //
    //    for x in res {
    //        println!("sorted nodes: {}", x)
    //    }
}
